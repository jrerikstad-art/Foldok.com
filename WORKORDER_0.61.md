# WORKORDER_0.61 — Nytt dokument fra chatten, format-valg, og direkte skisse

## Shipped
- A: `+ Nytt` shows errors; on 200 opens tab + Tools; format row; folder-safe create
- B: `doc.output_format` pdf|html|pptx|docx; «Eksporter som»; pptx splits tables with notice
- C: Hub «lag en installasjonsmanual» → folder-less project + document + «Velg mappe» banner
- C3: `primary_folder` / `project_state_load|save` — memory state until folder bound
- D: `/api/doc/sketch/upsert|move|delete` — zero-token; client wired

## Regression
`test_69_wo061_folderless_format_zero_token_sketch` — suite = **69**.
