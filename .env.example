# Copy this file to .env and paste your Anthropic API key.
# .env is local only — never commit it.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Paid export wallet (off by default while developing). Set to 1 to charge again.
# FOLDOK_EXPORT_PRICE=1
